# Adv360-Pro-ZMK

Las instrucciones para agentes viven en **[`AGENTS.md`](AGENTS.md)**, que es la
convención que leen todos los agentes y no solo Claude.

Leelo antes de tocar `hostsync/` o `config/`.

Este archivo es solo un puntero a propósito: mantener dos copias de las mismas
instrucciones garantiza que tarde o temprano se desalineen.
